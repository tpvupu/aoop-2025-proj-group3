# 網頁版遊戲結構圖

## 📂 文件結構樹

```
web/
│
├── 📄 index.html                 # 主頁面入口
│
├── 📚 文檔
│   ├── README.md                 # 項目說明
│   ├── DEPLOYMENT.md             # 部署指南  
│   ├── TESTING.md                # 測試指南
│   └── 轉換完成報告.md           # 完成報告
│
├── 🚀 start.sh                   # 快速啟動腳本
│
└── js/                           # JavaScript 代碼
    │
    ├── main.js                   # 遊戲入口和全局狀態
    ├── config.js                 # 遊戲配置和工具函數
    │
    ├── models/                   # 數據模型
    │   └── Character.js          # 角色類別
    │
    └── scenes/                   # 遊戲場景
        ├── BootScene.js          # 啟動場景
        ├── PreloadScene.js       # 資源載入
        ├── FirstScene.js         # 歡迎畫面
        ├── StartScene.js         # 開始選單
        ├── IntroScene.js         # 遊戲說明
        ├── CharacterSelectScene.js   # 角色選擇
        ├── StoryScene.js         # 劇情推進
        ├── MainScene.js          # 主遊戲畫面
        ├── EventScene.js         # 事件選擇
        ├── DiaryScene.js         # 日記系統
        ├── RankScene.js          # 排行榜
        └── EndScene.js           # 結束畫面
```

---

## 🎮 遊戲流程圖

```
┌─────────────────┐
│   index.html    │ 載入頁面
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   BootScene     │ 初始化
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ PreloadScene    │ 載入資源
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  FirstScene     │ 歡迎畫面
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌──────┐  ┌──────────┐
│ Intro│  │StartScene│ 開始選單
└──────┘  └─────┬────┘
                │
                ▼
    ┌───────────────────────┐
    │CharacterSelectScene   │ 選擇角色
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │     StoryScene        │ 劇情
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │    EventScene         │ 事件選擇
    └───────────┬───────────┘
                │
                ▼
    ┌───────────────────────┐
    │    MainScene  ◄───────┤ 主畫面
    └───────────┬───────────┘
                │
        ┌───────┼───────┐
        │       │       │
        ▼       ▼       ▼
    ┌─────┐ ┌─────┐ ┌────────┐
    │Diary│ │下週 │ │考試週  │
    └─────┘ └──┬──┘ └───┬────┘
               │        │
               └────┬───┘
                    │
                    ▼
            ┌───────────────┐
            │   EndScene    │ 結束
            └───────┬───────┘
                    │
            ┌───────┼───────┐
            │       │       │
            ▼       ▼       ▼
        ┌─────┐ ┌─────┐ ┌─────┐
        │排行榜│ │重玩 │ │主選單│
        └─────┘ └─────┘ └─────┘
```

---

## 🔗 模組依賴關係

```
index.html
    │
    ├──> Phaser 3 (CDN)
    │
    ├──> config.js
    │    └──> GameConfig
    │    └──> GameUtils
    │
    ├──> models/Character.js
    │    └──> Character class
    │
    ├──> scenes/*.js (所有場景)
    │
    └──> main.js
         ├──> phaserConfig
         ├──> GameState
         └──> new Phaser.Game()
```

---

## 🎯 核心類別關係

```
┌──────────────────────────┐
│      GameState           │ 全局狀態管理
│  ─────────────────────   │
│  + player: Character     │
│  + currentWeek: number   │
│  + eventsData: Object    │
│  + bgmEnabled: boolean   │
│  + sfxEnabled: boolean   │
│  ─────────────────────   │
│  + setPlayer()           │
│  + getPlayer()           │
│  + reset()               │
└────────┬─────────────────┘
         │
         │ 使用
         ▼
┌──────────────────────────┐
│      Character           │ 角色類別
│  ─────────────────────   │
│  + name: string          │
│  + intelligence: number  │
│  + mood: number          │
│  + energy: number        │
│  + social: number        │
│  + knowledge: number     │
│  ─────────────────────   │
│  + study()               │
│  + socialize()           │
│  + playGame()            │
│  + rest()                │
│  + calculateGPA()        │
└──────────────────────────┘
         ▲
         │ 繼承
         │
┌──────────────────────────┐
│   Phaser.Scene           │ Phaser 場景基類
│  ─────────────────────   │
│  + create()              │
│  + update()              │
│  + preload()             │
└────────┬─────────────────┘
         │
         │ 繼承
         ▼
┌──────────────────────────┐
│   所有場景類             │
│  ─────────────────────   │
│  - BootScene             │
│  - PreloadScene          │
│  - FirstScene            │
│  - StartScene            │
│  - ...                   │
└──────────────────────────┘
```

---

## 📊 數據流向

```
用戶操作
    │
    ▼
┌─────────────┐
│ 場景事件     │ (點擊、選擇)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Character   │ (更新屬性)
│   方法調用   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ GameState   │ (保存狀態)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 場景切換     │ (渲染新場景)
└─────────────┘
```

---

## 🎨 場景分類

### 導航場景 (Navigation)
- FirstScene - 歡迎
- StartScene - 選單
- IntroScene - 說明

### 遊戲場景 (Gameplay)
- CharacterSelectScene - 角色選擇
- StoryScene - 劇情
- MainScene - 主畫面
- EventScene - 事件選擇

### 輔助場景 (Auxiliary)
- DiaryScene - 日記
- RankScene - 排行榜

### 結束場景 (Ending)
- EndScene - 結算

### 系統場景 (System)
- BootScene - 啟動
- PreloadScene - 載入

---

## 🔧 擴展指南

### 添加新場景
1. 在 `js/scenes/` 創建新文件
2. 繼承 `Phaser.Scene`
3. 實現 `create()` 方法
4. 在 `index.html` 中引入
5. 在 `main.js` 的 scene 數組中註冊

### 添加新角色
1. 在 `config.js` 的 `GameConfig.characters` 中添加
2. 準備角色圖片資源
3. 更新 `CharacterSelectScene.js`

### 添加新事件
1. 修改事件數據（events.json）
2. 確保格式正確
3. 在 `PreloadScene.js` 中載入

---

**這個結構圖幫助你快速理解整個項目！** 📚✨
