# 今天的我也想耍廢 - 網頁版

🎮 **Lazy Me Today Too - Web Edition**

這是「今天的我也想耍廢」遊戲的 HTML5 網頁版本，使用 Phaser 3 遊戲框架開發。

## 📋 目錄結構

```
web/
├── index.html              # 主頁面
├── js/
│   ├── main.js            # 遊戲入口
│   ├── config.js          # 遊戲配置
│   ├── models/
│   │   └── Character.js   # 角色類別
│   └── scenes/
│       ├── BootScene.js          # 啟動場景
│       ├── PreloadScene.js       # 資源載入
│       ├── FirstScene.js         # 歡迎畫面
│       ├── StartScene.js         # 開始選單
│       ├── IntroScene.js         # 遊戲說明
│       ├── CharacterSelectScene.js  # 角色選擇
│       ├── StoryScene.js         # 劇情場景
│       ├── MainScene.js          # 主遊戲場景
│       ├── EventScene.js         # 事件選擇
│       ├── DiaryScene.js         # 日記本
│       ├── RankScene.js          # 排行榜
│       └── EndScene.js           # 結束場景
└── README.md              # 說明文件
```

## 🚀 快速開始

### 方法一：直接使用瀏覽器（推薦用於測試）

由於瀏覽器的同源政策限制，需要運行一個本地 Web 伺服器：

#### 使用 Python（推薦）
```bash
cd web
python3 -m http.server 8000
```

然後在瀏覽器打開：`http://localhost:8000`

#### 使用 Node.js
```bash
cd web
npx http-server -p 8000
```

#### 使用 VS Code
安裝 "Live Server" 擴充套件，右鍵點擊 `index.html` 選擇 "Open with Live Server"

### 方法二：部署到 GitHub Pages

1. 在 GitHub 創建新的 repository
2. 上傳 `web` 目錄下的所有文件
3. 在 repository 設定中啟用 GitHub Pages
4. 選擇分支和根目錄
5. 訪問 `https://your-username.github.io/your-repo-name/`

### 方法三：部署到 Netlify

1. 註冊 [Netlify](https://www.netlify.com/) 帳號
2. 將 `web` 目錄拖放到 Netlify 的部署區
3. 等待部署完成
4. 獲取訪問連結

## 🎯 遊戲特色

✅ **完全網頁化** - 無需安裝，瀏覽器直接運行  
✅ **響應式設計** - 支持不同螢幕尺寸  
✅ **保留核心玩法** - 完整移植 Python 版本的遊戲邏輯  
✅ **現代化 UI** - 使用 Phaser 3 打造流暢體驗  
✅ **跨平台** - Windows、Mac、Linux 皆可運行  

## 🎮 遊戲玩法

1. **選擇角色**：從四個性格不同的角色中選擇
2. **做出選擇**：每週面臨不同事件，選擇你的行動
3. **平衡屬性**：管理心情、體力、社交和知識四大屬性
4. **應對考試**：第 8 週期中考、第 16 週期末考
5. **獲得成績**：根據表現計算最終 GPA

## 📝 與 Python 版本的差異

| 功能 | Python 版 | 網頁版 |
|------|----------|--------|
| 運行環境 | 需要 Python + Pygame | 瀏覽器即可 |
| 圖片/動畫 | GIF 動畫 | 暫用純色替代（可擴展）|
| 音效 | 完整音效系統 | 預留介面（待整合）|
| AI 建議 | OpenAI API 整合 | 預留功能 |
| 存檔系統 | 本地文件 | LocalStorage |
| 部署 | 需打包成執行檔 | 直接託管網頁 |

## 🔧 自定義與擴展

### 添加真實圖片資源

1. 將圖片放到 `web/assets/images/` 目錄
2. 在 `PreloadScene.js` 中載入：
```javascript
this.load.image('background', 'assets/images/background.png');
```

3. 在場景中使用：
```javascript
this.add.image(600, 400, 'background');
```

### 整合原有的 events.json

1. 將 `event/events.json` 複製到 `web/` 目錄
2. 在 `PreloadScene.js` 中載入：
```javascript
this.load.json('events', 'events.json');
```

3. 在 create() 中使用：
```javascript
const eventsData = this.cache.json.get('events');
window.GameState.setEventsData(eventsData);
```

### 添加音效

1. 將音效文件放到 `web/assets/sounds/`
2. 在 `PreloadScene.js` 載入：
```javascript
this.load.audio('bgm', 'assets/sounds/bgm.mp3');
```

3. 播放音效：
```javascript
this.sound.play('bgm', { loop: true });
```

## 🐛 已知問題與待辦

- [ ] 圖片資源需要手動整合
- [ ] 音效系統需要完善
- [ ] GIF 動畫轉換為精靈圖
- [ ] 添加角色動畫效果
- [ ] 實現 LocalStorage 存檔
- [ ] 連接後端 API（排行榜）
- [ ] 移動端觸控優化
- [ ] AI 建議功能整合

## 📱 瀏覽器兼容性

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ⚠️ IE 不支持

## 📄 授權

與原 Python 版本相同的授權條款。

## 👥 開發者

- NYCU_EE [113511116 tpvupu](https://github.com/tpvupu) : 陳欣怡
- NYCU_EE [113511266 xiaotin22](https://github.com/xiaotin22)：楊庭瑞

---

## 💡 技術細節

### 使用的技術

- **Phaser 3.70.0** - HTML5 遊戲框架
- **純 JavaScript** - 無需編譯，直接運行
- **Canvas API** - 高性能圖形渲染
- **LocalStorage** - 本地數據存儲

### 架構設計

- **場景系統**：每個遊戲畫面獨立成場景
- **全局狀態**：使用 GameState 管理遊戲數據
- **角色系統**：完整移植 Python 版的角色邏輯
- **事件驅動**：基於 Phaser 的事件系統

### 性能優化

- CDN 載入 Phaser 框架
- 資源預載入機制
- 場景切換流暢過渡
- 內存管理與資源釋放

## 🎓 學習資源

- [Phaser 3 官方文檔](https://photonstorm.github.io/phaser3-docs/)
- [Phaser 3 範例](https://phaser.io/examples)
- [遊戲開發教程](https://phaser.io/tutorials)

---

**享受遊戲！Have Fun! 🎮**
